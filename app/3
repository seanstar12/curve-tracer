import Ember from 'ember';

export default Ember.Controller.extend({
  types: ['MPN BJT', 'PNP BJT', 'diode', 'zenier diode'],
  selectedType: 'PNP BJT',

  incrementMin: 0.001,
  incrementMax: 1,
  incrementStep: 0.001,
  selectedIncrement: 1,

  voltageRangeMin: 1,
  voltageRangeMax: 100,
  voltageRangeStep: 0.1,
  selectedVoltageRangeFrom: 9,
  selectedVoltageRangeTo: 26,
});
